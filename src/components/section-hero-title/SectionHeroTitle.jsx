// Styles
import "./section-hero-title.css";

export default function SectionHeroTitle({ children }) {
    return (
        <h2 className="heading-2 section-hero-title">
            {children}
        </h2>
    )
}